---
phase: 01-backend-foundation-on-api-shineee-space
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/shine-app/backend/main.py
  - ~/shine-app/backend/auth.py
  - ~/shine-app/backend/models.py
  - ~/shine-app/backend/requirements.txt
  - ~/shine-app/backend/.env.example
  - ~/shine-app/backend/services/db.py
  - ~/shine-app/backend/services/xui.py
  - ~/shine-app/backend/__init__.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-05
  - API-04
  - API-05
  - API-06
  - API-07

must_haves:
  truths:
    - "auth.py содержит функцию validate_init_data, которая отклоняет запросы без hash с 401"
    - "auth.py отклоняет initData старше 24 часов с 401"
    - "auth.py извлекает tg_user_id только из валидированного initData (никогда из тела/query)"
    - "main.py содержит CORSMiddleware с allow_origin_regex для *.vercel.app"
    - "main.py содержит slowapi rate limiter 60/minute по tg_user_id"
    - "services/db.py и services/xui.py скопированы из ~/vpn-bot/services/"
    - "models.py содержит Pydantic схемы UserProfile и ConfigResponse"
  artifacts:
    - path: "~/shine-app/backend/auth.py"
      provides: "Telegram initData HMAC-SHA256 validation dependency"
      exports: ["validate_init_data", "get_user_id_for_limiter"]
    - path: "~/shine-app/backend/models.py"
      provides: "Pydantic response schemas"
      exports: ["UserProfile", "ConfigResponse", "HealthResponse"]
    - path: "~/shine-app/backend/main.py"
      provides: "FastAPI app с CORS + rate limiting"
      exports: ["app", "limiter"]
    - path: "~/shine-app/backend/services/db.py"
      provides: "SQLite access functions (скопировано из vpn-bot)"
    - path: "~/shine-app/backend/services/xui.py"
      provides: "x-ui API client (скопировано из vpn-bot)"
    - path: "~/shine-app/backend/requirements.txt"
      provides: "Python dependencies"
      contains: "fastapi\nuvicorn\nslowapi\nhttpx\npython-dotenv"
  key_links:
    - from: "auth.py validate_init_data"
      to: "os.environ[BOT_TOKEN]"
      via: "hmac.new(b'WebAppData', bot_token.encode(), hashlib.sha256)"
      pattern: "hmac\\.compare_digest"
    - from: "main.py limiter"
      to: "auth.py get_user_id_for_limiter"
      via: "Limiter(key_func=get_user_id_for_limiter)"
      pattern: "Limiter\\(key_func="
---

<objective>
Создать скелет FastAPI-приложения: структура проекта, HMAC auth middleware, Pydantic модели, CORS + rate limiting в main.py, скопировать services из vpn-bot.

Purpose: Роутеры (Plan 02) смогут использовать validate_init_data dependency и Pydantic модели без повторной реализации. Это контрактный слой для всего backend.

Output: Рабочий FastAPI app, который стартует командой uvicorn main:app, возвращает 401 на неавторизованные запросы, имеет /docs, CORS и rate limiting.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-backend-foundation-on-api-shineee-space/01-CONTEXT.md
@.planning/phases/01-backend-foundation-on-api-shineee-space/01-RESEARCH.md

<interfaces>
<!-- Контракты, которые этот план СОЗДАЁТ и которые Plan 02 будет использовать -->

Из auth.py (будет создан):
```python
# FastAPI Dependency — инжектируется в защищённые роутеры
def validate_init_data(x_telegram_init_data: str = Header(...)) -> dict:
    """
    Возвращает dict с полями: id (int), first_name (str), last_name (str, opt), username (str, opt)
    Выбрасывает HTTPException(401) при невалидном/истёкшем initData
    Выбрасывает HTTPException(401) при отсутствии hash
    """

# Для slowapi key_func
def get_user_id_for_limiter(request: Request) -> str:
    """Возвращает str(tg_user_id) из request.state или IP как fallback"""
```

Из models.py (будет создан):
```python
class UserProfile(BaseModel):
    tg_user_id: int
    name: str
    subscription_active: bool
    subscription_end: Optional[date]  # None если никогда не было
    device_limit: int

class ConfigResponse(BaseModel):
    vless_url: str

class HealthResponse(BaseModel):
    status: str  # всегда "ok"
```

Из main.py (будет создан):
```python
app: FastAPI  # инстанс приложения
limiter: Limiter  # slowapi limiter для @limiter.limit("60/minute") в роутерах
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Прочитать существующие services и скопировать в backend</name>
  <read_first>
    - ~/vpn-bot/services/db.py (КРИТИЧНО — понять функции и зависимости до копирования)
    - ~/vpn-bot/services/xui.py (КРИТИЧНО — понять env vars и HTTP-клиент sync/async)
    - ~/vpn-bot/.env (если существует — выявить имена переменных для xui.py)
    - ~/vpn-bot/requirements.txt (понять какие библиотеки использует xui.py)
  </read_first>
  <files>
    ~/shine-app/backend/services/__init__.py
    ~/shine-app/backend/services/db.py
    ~/shine-app/backend/services/xui.py
    ~/shine-app/backend/requirements.txt
    ~/shine-app/backend/.env.example
  </files>
  <action>
  1. Создать директории на VPS (через SSH):
     ```
     mkdir -p ~/shine-app/backend/services
     mkdir -p ~/shine-app/backend/routers
     touch ~/shine-app/backend/services/__init__.py
     touch ~/shine-app/backend/__init__.py
     ```

  2. Скопировать services из vpn-bot (per D-03):
     ```
     cp ~/vpn-bot/services/db.py ~/shine-app/backend/services/db.py
     cp ~/vpn-bot/services/xui.py ~/shine-app/backend/services/xui.py
     ```

  3. Прочитать скопированные файлы и выявить:
     - Все функции в db.py (особенно: get_user_by_tg_id или аналог, check_subscription или аналог)
     - Все env vars в xui.py (переменные окружения через os.getenv или os.environ)
     - Использует ли xui.py синхронный requests или async httpx

  4. Если xui.py использует синхронный requests — добавить в requirements.txt строку requests.
     Если использует httpx — добавить httpx.
     Если xui.py не async — НЕ менять его сейчас, оборачивать вызовы в run_in_executor в роутере.

  5. Создать requirements.txt:
     ```
     fastapi[standard]==0.136.1
     uvicorn==0.47.0
     slowapi==0.1.9
     python-dotenv==1.2.2
     httpx==0.28.1
     ```
     (добавить requests==2.32.3 если xui.py его использует)

  6. Создать .env.example — ПОЛНЫЙ список переменных:
     ```
     # Telegram
     BOT_TOKEN=your_telegram_bot_token_here

     # SQLite database (read-only, per D-04)
     DB_PATH=/root/vpn-bot/users.db

     # x-ui panel (значения выявить из xui.py)
     XUI_BASE_URL=http://127.0.0.1:21008
     XUI_PASSWORD=your_xui_admin_password
     XUI_INBOUND_ID=2

     # Добавить любые другие env vars выявленные из xui.py
     ```
     ВАЖНО: добавить в .env.example ВСЕ переменные, найденные в xui.py через os.getenv/os.environ.
  </action>
  <verify>
    <automated>
    ssh root@205.172.56.163 "test -f ~/shine-app/backend/services/db.py && test -f ~/shine-app/backend/services/xui.py && echo 'services OK' || echo 'MISSING'"
    ssh root@205.172.56.163 "grep -c 'def ' ~/shine-app/backend/services/db.py"
    ssh root@205.172.56.163 "cat ~/shine-app/backend/requirements.txt"
    ssh root@205.172.56.163 "cat ~/shine-app/backend/.env.example"
    </automated>
  </verify>
  <acceptance_criteria>
  - ~/shine-app/backend/services/db.py существует и идентичен ~/vpn-bot/services/db.py
  - ~/shine-app/backend/services/xui.py существует и идентичен ~/vpn-bot/services/xui.py
  - requirements.txt содержит строку "fastapi[standard]"
  - requirements.txt содержит строку "slowapi"
  - .env.example содержит "BOT_TOKEN="
  - .env.example содержит "DB_PATH="
  - .env.example содержит "XUI_BASE_URL="
  - .env.example содержит "XUI_INBOUND_ID="
  </acceptance_criteria>
  <done>services/ скопированы, requirements.txt создан, .env.example содержит все env vars из xui.py</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Создать auth.py (HMAC валидация) и models.py</name>
  <read_first>
    - ~/shine-app/backend/services/db.py (понять имена функций перед написанием auth.py)
    - ~/shine-app/backend/services/xui.py (понять имена функций)
    - .planning/phases/01-backend-foundation-on-api-shineee-space/01-RESEARCH.md (Pattern 1 — алгоритм валидации)
  </read_first>
  <behavior>
    - validate_init_data с корректным hash и auth_date в пределах 24ч → возвращает dict с ключом "id"
    - validate_init_data без hash → HTTPException(401, detail="Missing hash")
    - validate_init_data с неверным hash → HTTPException(401, detail="Invalid initData signature")
    - validate_init_data с auth_date старше 86400 секунд → HTTPException(401, detail="initData expired")
    - validate_init_data с дважды URL-encoded данными → корректно декодирует через unquote() и валидирует
    - get_user_id_for_limiter при наличии tg_user_id в request.state → возвращает str(tg_user_id)
  </behavior>
  <files>
    ~/shine-app/backend/auth.py
    ~/shine-app/backend/models.py
    ~/shine-app/backend/tests/test_auth.py
  </files>
  <action>
  1. Создать ~/shine-app/backend/auth.py:

  ```python
  import hashlib
  import hmac
  import json
  import os
  import time
  from urllib.parse import unquote, parse_qsl

  from fastapi import Header, HTTPException, Request


  def validate_init_data(x_telegram_init_data: str = Header(...)) -> dict:
      """
      Валидирует Telegram initData (AUTH-01, AUTH-03, AUTH-05).
      Возвращает user dict из initData (содержит 'id', 'first_name' и др.).
      Выбрасывает HTTPException(401) при невалидных данных.
      tg_user_id берётся ТОЛЬКО из этого валидированного dict — никогда из тела запроса.
      """
      try:
          parsed = dict(parse_qsl(unquote(x_telegram_init_data), keep_blank_values=True))
      except Exception:
          raise HTTPException(status_code=401, detail="Invalid initData format")

      received_hash = parsed.pop("hash", None)
      if not received_hash:
          raise HTTPException(status_code=401, detail="Missing hash")

      data_check_string = "\n".join(
          f"{k}={v}" for k, v in sorted(parsed.items())
      )

      bot_token = os.environ["BOT_TOKEN"]
      secret_key = hmac.new(b"WebAppData", bot_token.encode(), hashlib.sha256).digest()
      computed_hash = hmac.new(
          secret_key, data_check_string.encode(), hashlib.sha256
      ).hexdigest()

      if not hmac.compare_digest(computed_hash, received_hash):
          raise HTTPException(status_code=401, detail="Invalid initData signature")

      auth_date = int(parsed.get("auth_date", 0))
      if time.time() - auth_date > 86400:
          raise HTTPException(status_code=401, detail="initData expired")

      user_data = json.loads(parsed.get("user", "{}"))
      return user_data


  def get_user_id_for_limiter(request: Request) -> str:
      """
      Ключ для slowapi rate limiter.
      Возвращает str(tg_user_id) если он сохранён в request.state, иначе IP.
      """
      tg_user_id = getattr(request.state, "tg_user_id", None)
      if tg_user_id:
          return str(tg_user_id)
      return request.client.host if request.client else "unknown"
  ```

  2. Создать ~/shine-app/backend/models.py:

  ```python
  from datetime import date
  from typing import Optional

  from pydantic import BaseModel


  class UserProfile(BaseModel):
      """Ответ GET /api/me (D-05)"""
      tg_user_id: int
      name: str
      subscription_active: bool
      subscription_end: Optional[date] = None
      device_limit: int


  class ConfigResponse(BaseModel):
      """Ответ GET /api/config (D-06)"""
      vless_url: str


  class HealthResponse(BaseModel):
      """Ответ GET /api/health"""
      status: str  # всегда "ok"
  ```

  3. Создать ~/shine-app/backend/tests/__init__.py (пустой).

  4. Создать ~/shine-app/backend/tests/test_auth.py:

  ```python
  import hashlib
  import hmac
  import json
  import os
  import time
  import urllib.parse

  import pytest
  from fastapi import HTTPException
  from unittest.mock import patch


  def make_init_data(bot_token: str, user_id: int = 123456789, age_seconds: int = 0) -> str:
      """Генерирует валидный initData для тестов."""
      auth_date = int(time.time()) - age_seconds
      user = json.dumps({"id": user_id, "first_name": "Test", "is_bot": False})
      params = {
          "auth_date": str(auth_date),
          "user": user,
      }
      data_check_string = "\n".join(f"{k}={v}" for k, v in sorted(params.items()))
      secret_key = hmac.new(b"WebAppData", bot_token.encode(), hashlib.sha256).digest()
      params["hash"] = hmac.new(
          secret_key, data_check_string.encode(), hashlib.sha256
      ).hexdigest()
      return urllib.parse.urlencode(params)


  @patch.dict(os.environ, {"BOT_TOKEN": "test_bot_token_123"})
  def test_valid_init_data():
      from auth import validate_init_data
      init_data = make_init_data("test_bot_token_123")
      result = validate_init_data(x_telegram_init_data=init_data)
      assert result["id"] == 123456789
      assert result["first_name"] == "Test"


  @patch.dict(os.environ, {"BOT_TOKEN": "test_bot_token_123"})
  def test_missing_hash():
      from auth import validate_init_data
      with pytest.raises(HTTPException) as exc:
          validate_init_data(x_telegram_init_data="auth_date=1234567890&user=%7B%7D")
      assert exc.value.status_code == 401
      assert "Missing hash" in exc.value.detail


  @patch.dict(os.environ, {"BOT_TOKEN": "test_bot_token_123"})
  def test_invalid_signature():
      from auth import validate_init_data
      init_data = make_init_data("test_bot_token_123")
      tampered = init_data.replace("hash=", "hash=tampered")
      with pytest.raises(HTTPException) as exc:
          validate_init_data(x_telegram_init_data=tampered)
      assert exc.value.status_code == 401
      assert "Invalid initData signature" in exc.value.detail


  @patch.dict(os.environ, {"BOT_TOKEN": "test_bot_token_123"})
  def test_expired_init_data():
      from auth import validate_init_data
      # 25 часов назад
      init_data = make_init_data("test_bot_token_123", age_seconds=90000)
      with pytest.raises(HTTPException) as exc:
          validate_init_data(x_telegram_init_data=init_data)
      assert exc.value.status_code == 401
      assert "expired" in exc.value.detail


  @patch.dict(os.environ, {"BOT_TOKEN": "test_bot_token_123"})
  def test_url_encoded_init_data():
      """initData может быть URL-encoded — должен корректно декодироваться."""
      from auth import validate_init_data
      init_data = make_init_data("test_bot_token_123")
      double_encoded = urllib.parse.quote(init_data)
      result = validate_init_data(x_telegram_init_data=double_encoded)
      assert result["id"] == 123456789
  ```

  5. Установить зависимости и запустить тесты:
  ```bash
  cd ~/shine-app/backend
  python3 -m venv venv
  source venv/bin/activate
  pip install "fastapi[standard]==0.136.1" slowapi==0.1.9 python-dotenv==1.2.2 httpx==0.28.1 pytest
  cd ~/shine-app/backend && python3 -m pytest tests/test_auth.py -v
  ```

  Все 5 тестов должны пройти (GREEN) перед завершением задачи.
  </action>
  <verify>
    <automated>
    ssh root@205.172.56.163 "cd ~/shine-app/backend && source venv/bin/activate && python3 -m pytest tests/test_auth.py -v 2>&1"
    </automated>
  </verify>
  <acceptance_criteria>
  - ~/shine-app/backend/auth.py существует
  - auth.py содержит "def validate_init_data("
  - auth.py содержит "hmac.compare_digest"
  - auth.py содержит "initData expired"
  - auth.py содержит "unquote("
  - auth.py НЕ содержит "user_id" как параметр функции (user_id только из initData)
  - ~/shine-app/backend/models.py существует
  - models.py содержит "class UserProfile("
  - models.py содержит "class ConfigResponse("
  - models.py содержит "subscription_active: bool"
  - ~/shine-app/backend/tests/test_auth.py существует
  - pytest tests/test_auth.py завершается с кодом 0 (все тесты GREEN)
  </acceptance_criteria>
  <done>auth.py с HMAC валидацией создан, все 5 тестов проходят, models.py содержит UserProfile и ConfigResponse</done>
</task>

<task type="auto">
  <name>Task 3: Создать main.py (FastAPI app с CORS + rate limiting)</name>
  <read_first>
    - ~/shine-app/backend/auth.py (использовать get_user_id_for_limiter — уже создан в Task 2)
    - ~/shine-app/backend/models.py (убедиться что импорты корректны)
    - .planning/phases/01-backend-foundation-on-api-shineee-space/01-RESEARCH.md (Pattern 5 CORS, Pattern 2 slowapi)
  </read_first>
  <files>
    ~/shine-app/backend/main.py
    ~/shine-app/backend/routers/__init__.py
  </files>
  <action>
  1. Создать ~/shine-app/backend/routers/__init__.py (пустой).

  2. Создать ~/shine-app/backend/main.py:

  ```python
  from fastapi import FastAPI
  from fastapi.middleware.cors import CORSMiddleware
  from slowapi import Limiter, _rate_limit_exceeded_handler
  from slowapi.errors import RateLimitExceeded
  from slowapi.middleware import SlowAPIMiddleware

  from auth import get_user_id_for_limiter

  # Rate limiter по tg_user_id (per API-07)
  limiter = Limiter(key_func=get_user_id_for_limiter)

  app = FastAPI(
      title="Shine App API",
      version="1.0.0",
      description="Backend API for Shine VPN Telegram Mini App",
  )

  # slowapi middleware
  app.state.limiter = limiter
  app.add_exception_handler(RateLimitExceeded, _rate_limit_exceeded_handler)
  app.add_middleware(SlowAPIMiddleware)

  # CORS (per API-06): только Vercel + localhost для разработки
  # ВАЖНО: allow_origins с wildcard *.vercel.app НЕ работает в starlette
  # Используем allow_origin_regex (см. RESEARCH.md Pitfall 2)
  app.add_middleware(
      CORSMiddleware,
      allow_origins=["http://localhost:5173", "http://localhost:3000"],
      allow_origin_regex=r"https://.*\.vercel\.app",
      allow_credentials=True,
      allow_methods=["GET", "OPTIONS"],
      allow_headers=["X-Telegram-Init-Data", "Content-Type"],
  )

  # Роутеры подключаются после создания файлов в Plan 02
  # from routers import health, me, config
  # app.include_router(health.router, prefix="/api")
  # app.include_router(me.router, prefix="/api")
  # app.include_router(config.router, prefix="/api")

  # Временный health endpoint для проверки что app стартует
  from fastapi import Request

  @app.get("/api/health")
  async def health_check():
      return {"status": "ok"}
  ```

  ВАЖНО: закомментированные строки include_router будут раскомментированы в Plan 02
  после создания роутеров. Временный /api/health оставить для верификации.

  3. Проверить что app стартует:
  ```bash
  cd ~/shine-app/backend
  source venv/bin/activate
  python3 -c "from main import app; print('app OK')"
  uvicorn main:app --host 127.0.0.1 --port 8000 &
  sleep 2
  curl -s http://127.0.0.1:8000/api/health
  # ожидается: {"status":"ok"}
  kill %1
  ```
  </action>
  <verify>
    <automated>
    ssh root@205.172.56.163 "cd ~/shine-app/backend && source venv/bin/activate && python3 -c 'from main import app; print(\"app OK\")'"
    ssh root@205.172.56.163 "cd ~/shine-app/backend && source venv/bin/activate && uvicorn main:app --host 127.0.0.1 --port 8000 & sleep 2 && curl -s http://127.0.0.1:8000/api/health && kill %1"
    </automated>
  </verify>
  <acceptance_criteria>
  - ~/shine-app/backend/main.py существует
  - main.py содержит "CORSMiddleware"
  - main.py содержит "allow_origin_regex"
  - main.py содержит "*.vercel.app" (в строке регекспа)
  - main.py содержит "SlowAPIMiddleware"
  - main.py содержит "Limiter(key_func=get_user_id_for_limiter)"
  - main.py НЕ содержит 'allow_origins=["*"]'
  - `python3 -c 'from main import app; print("app OK")'` завершается без ошибок
  - curl http://127.0.0.1:8000/api/health возвращает {"status":"ok"}
  </acceptance_criteria>
  <done>main.py создан, app импортируется без ошибок, временный /api/health отвечает 200</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Telegram SDK → Backend | initData передаётся из фронта в X-Telegram-Init-Data header; фронт не доверенный |
| Backend → SQLite | Read-only доступ к ~/vpn-bot/users.db; параллельная запись от бота |
| Backend → x-ui API | Внутренний HTTP к 127.0.0.1:21008; только бэкенд имеет доступ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | validate_init_data | mitigate | hmac.compare_digest() предотвращает timing attack; HMAC-SHA256 с bot_token делает подделку вычислительно невозможной |
| T-01-02 | Tampering | initData hash | mitigate | Любое изменение данных делает hash невалидным; сравнение через hmac.compare_digest |
| T-01-03 | Repudiation | auth_date проверка | mitigate | Отклонение initData старше 24ч (86400 сек) предотвращает replay attacks с перехваченными данными |
| T-01-04 | Information Disclosure | tg_user_id из body/query | mitigate | auth.py извлекает user_id ТОЛЬКО из валидированного initData; роутеры получают user dict через Depends(validate_init_data) |
| T-01-05 | Denial of Service | API endpoints без rate limit | mitigate | slowapi limiter 60/minute per tg_user_id на всех защищённых endpoints |
| T-01-06 | Elevation of Privilege | services/db.py SQLite write | mitigate | Backend открывает users.db только для чтения (WAL mode); write операции только через бот |
| T-01-07 | Information Disclosure | .env файл в git | mitigate | .env добавлен в .gitignore; systemd читает через EnvironmentFile с абсолютным путём; только .env.example в git |
| T-01-08 | Spoofing | BOT_TOKEN отсутствует в env | mitigate | os.environ["BOT_TOKEN"] (не getenv) — KeyError при старте если переменная отсутствует; fail-fast |
</threat_model>

<verification>
На VPS после выполнения плана проверить:

```bash
# 1. Services скопированы
diff ~/vpn-bot/services/db.py ~/shine-app/backend/services/db.py

# 2. Auth тесты зелёные
cd ~/shine-app/backend && source venv/bin/activate && python3 -m pytest tests/test_auth.py -v

# 3. App стартует
python3 -c "from main import app; print('OK')"

# 4. Health endpoint
uvicorn main:app --host 127.0.0.1 --port 8000 &
curl -s http://127.0.0.1:8000/api/health
# {"status":"ok"}
kill %1

# 5. Без initData — 422 (FastAPI required header)
curl -s http://127.0.0.1:8000/api/me
# {"detail":[{"type":"missing","loc":["header","x-telegram-init-data"],...}]}
```
</verification>

<success_criteria>
- services/db.py и services/xui.py скопированы из ~/vpn-bot/services/ (per D-03)
- auth.py содержит validate_init_data с HMAC-SHA256 + auth_date проверкой (AUTH-01, AUTH-05)
- tg_user_id извлекается ТОЛЬКО из валидированного initData, никогда из тела (AUTH-03)
- 5/5 тестов в tests/test_auth.py проходят
- main.py: CORS с allow_origin_regex для *.vercel.app, slowapi 60/min по user_id (API-06, API-07)
- app импортируется без ошибок, временный /health отвечает 200
- .env.example содержит все env vars из xui.py (per D-08)
</success_criteria>

<output>
После завершения создать `.planning/phases/01-backend-foundation-on-api-shineee-space/01-01-SUMMARY.md` со следующей структурой:
- Что сделано (artifacts created)
- Точные имена функций в db.py и xui.py (для Plan 02)
- Полный список env vars (из xui.py + db.py)
- Использует ли xui.py sync requests или async httpx
- Любые отклонения от плана
</output>
