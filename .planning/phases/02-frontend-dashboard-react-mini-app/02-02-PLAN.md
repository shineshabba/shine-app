---
phase: 02-frontend-dashboard-react-mini-app
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01-PLAN.md
files_modified:
  - frontend/src/types/api.ts
  - frontend/src/api/client.ts
  - frontend/src/components/LoadingScreen.tsx
  - frontend/src/components/ErrorScreen.tsx
  - frontend/src/components/UnregisteredScreen.tsx
  - frontend/src/components/ErrorBoundary.tsx
autonomous: true
requirements:
  - AUTH-02
  - AUTH-04
  - FE-06
  - FE-08
  - DASH-07

must_haves:
  truths:
    - "API клиент добавляет заголовок X-Telegram-Init-Data к каждому запросу"
    - "apiClient.getMe() возвращает UserProfile при 200, бросает ошибку с кодом при 401/403/404"
    - "LoadingScreen показывает CSS-спиннер без внешних библиотек"
    - "ErrorScreen показывает кнопку «Повторить» и принимает onRetry callback"
    - "UnregisteredScreen показывает «Пройдите /start в боте @shine_connect_bot»"
    - "ErrorBoundary перехватывает React ошибки и рендерит fallback"
  artifacts:
    - path: "frontend/src/types/api.ts"
      provides: "TypeScript типы, соответствующие Pydantic схемам бэкенда"
      exports: ["UserProfile", "ConfigResponse", "ApiError"]
    - path: "frontend/src/api/client.ts"
      provides: "API клиент с auth header"
      exports: ["apiClient"]
    - path: "frontend/src/components/LoadingScreen.tsx"
      provides: "Loading state компонент"
    - path: "frontend/src/components/ErrorScreen.tsx"
      provides: "Error state компонент"
    - path: "frontend/src/components/UnregisteredScreen.tsx"
      provides: "404 state компонент"
    - path: "frontend/src/components/ErrorBoundary.tsx"
      provides: "React ErrorBoundary компонент"
  key_links:
    - from: "frontend/src/api/client.ts"
      to: "https://api.shineee.space/api/me"
      via: "fetch + X-Telegram-Init-Data header"
      pattern: "X-Telegram-Init-Data"
    - from: "frontend/src/api/client.ts"
      to: "frontend/src/types/api.ts"
      via: "TypeScript import"
      pattern: "UserProfile"
---

<objective>
Создать API-клиент с auth-заголовком и экраны состояний (Loading, Error, Unregistered).

Purpose: API-клиент — единственная точка взаимодействия с бэкендом. Каждый запрос должен нести X-Telegram-Init-Data. Экраны состояний обеспечивают корректный UX при загрузке, ошибках и незарегистрированных юзерах.

Output:
- `src/types/api.ts` — TypeScript типы (зеркало Pydantic моделей из backend/models.py)
- `src/api/client.ts` — fetch-обёртка с auth-заголовком, обработкой ошибок
- `src/components/LoadingScreen.tsx` — CSS-спиннер, aria-label «Загрузка...»
- `src/components/ErrorScreen.tsx` — Lucide WifiOff + «Повторить» + onRetry callback
- `src/components/UnregisteredScreen.tsx` — Lucide UserX + инструкция /start
- `src/components/ErrorBoundary.tsx` — класс-компонент React ErrorBoundary
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-dashboard-react-mini-app/02-CONTEXT.md
@.planning/phases/02-frontend-dashboard-react-mini-app/02-UI-SPEC.md
@backend/models.py
@.planning/phases/01-backend-foundation-on-api-shineee-space/01-05-SUMMARY.md

<interfaces>
<!-- Pydantic схемы из backend/models.py — API контракт -->
```python
class UserProfile(BaseModel):
    tg_user_id: int
    name: str
    subscription_active: bool
    subscription_end: Optional[date] = None  # None если никогда не подписывался
    device_limit: int

class ConfigResponse(BaseModel):
    vless_url: str

class HealthResponse(BaseModel):
    status: str  # всегда "ok"
```

<!-- Эндпойнты бэкенда (из 01-05-SUMMARY.md) -->
- GET https://api.shineee.space/api/me — требует X-Telegram-Init-Data header
- GET https://api.shineee.space/api/config — требует X-Telegram-Init-Data, 403 если неактивна подписка
- 401 — невалидный initData
- 404 — пользователь не зарегистрирован
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать TypeScript типы и API-клиент с X-Telegram-Init-Data</name>
  <files>
    frontend/src/types/api.ts
    frontend/src/api/client.ts
  </files>
  <read_first>
    - backend/models.py (Pydantic схемы — источник истины для TypeScript типов)
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-CONTEXT.md (раздел code_context, specifics)
    - .planning/phases/01-backend-foundation-on-api-shineee-space/01-05-SUMMARY.md (эндпойнты и коды ошибок)
  </read_first>
  <action>
**frontend/src/types/api.ts** — TypeScript типы, зеркалящие Pydantic модели:
```typescript
/**
 * TypeScript типы для API контракта.
 * Зеркало Pydantic моделей из backend/models.py.
 * При изменении моделей — обновить здесь тоже.
 */

export interface UserProfile {
  tg_user_id: number
  name: string
  subscription_active: boolean
  subscription_end: string | null  // ISO date string "YYYY-MM-DD" или null
  device_limit: number
}

export interface ConfigResponse {
  vless_url: string
}

export interface HealthResponse {
  status: 'ok'
}

/** Коды ошибок API — используются для маршрутизации на нужный экран */
export type ApiErrorCode = 401 | 403 | 404 | 'network' | 'unknown'

export class ApiError extends Error {
  constructor(
    public readonly code: ApiErrorCode,
    message: string,
  ) {
    super(message)
    this.name = 'ApiError'
  }
}
```

**frontend/src/api/client.ts** — fetch-обёртка с авторизационным заголовком:
```typescript
import { UserProfile, ConfigResponse, ApiError, ApiErrorCode } from '../types/api'

const API_BASE = 'https://api.shineee.space'

/**
 * Получить initData из Telegram WebApp.
 * НИКОГДА не конструируем initData вручную — только из window.Telegram.WebApp.
 * Пустая строка означает запуск вне Telegram (dev-режим).
 */
function getInitData(): string {
  return window.Telegram?.WebApp?.initData ?? ''
}

async function apiFetch<T>(path: string): Promise<T> {
  const initData = getInitData()

  let response: Response
  try {
    response = await fetch(`${API_BASE}${path}`, {
      headers: {
        'X-Telegram-Init-Data': initData,
        'Content-Type': 'application/json',
      },
    })
  } catch {
    throw new ApiError('network', 'Нет соединения с сервером')
  }

  if (!response.ok) {
    const code = response.status as ApiErrorCode
    const knownCodes: ApiErrorCode[] = [401, 403, 404]
    const errorCode: ApiErrorCode = knownCodes.includes(code) ? code : 'unknown'

    switch (errorCode) {
      case 401:
        throw new ApiError(401, 'Не удалось авторизоваться')
      case 403:
        throw new ApiError(403, 'Подписка не активна')
      case 404:
        throw new ApiError(404, 'Пользователь не найден')
      default:
        throw new ApiError('unknown', `Ошибка сервера: ${response.status}`)
    }
  }

  return response.json() as Promise<T>
}

export const apiClient = {
  /**
   * GET /api/me — профиль пользователя.
   * Бросает ApiError(404) если не зарегистрирован.
   * Бросает ApiError(401) если невалидный initData.
   */
  getMe(): Promise<UserProfile> {
    return apiFetch<UserProfile>('/api/me')
  },

  /**
   * GET /api/config — VLESS конфигурация.
   * Бросает ApiError(403) если подписка не активна.
   */
  getConfig(): Promise<ConfigResponse> {
    return apiFetch<ConfigResponse>('/api/config')
  },
}
```

Добавить глобальный тип для Telegram WebApp в `frontend/src/vite-env.d.ts`:
```typescript
/// <reference types="vite/client" />

interface Window {
  Telegram?: {
    WebApp: {
      initData: string
      initDataUnsafe: Record<string, unknown>
      themeParams: Record<string, string>
      ready: () => void
      close: () => void
      openTelegramLink: (url: string) => void
    }
  }
}
```
  </action>
  <verify>
    <automated>cd /path/to/frontend && npm run typecheck 2>&amp;1; echo "Exit: $?"</automated>
  </verify>
  <done>
    - frontend/src/types/api.ts содержит UserProfile, ConfigResponse, ApiError с полем code
    - frontend/src/api/client.ts содержит apiClient.getMe() и apiClient.getConfig()
    - X-Telegram-Init-Data заголовок присутствует в apiFetch
    - npm run typecheck проходит без ошибок
  </done>
  <acceptance_criteria>
    - `grep 'X-Telegram-Init-Data' frontend/src/api/client.ts` возвращает совпадение
    - `grep 'subscription_active' frontend/src/types/api.ts` возвращает совпадение
    - `grep 'subscription_end: string | null' frontend/src/types/api.ts` возвращает совпадение
    - `grep 'vless_url' frontend/src/types/api.ts` возвращает совпадение
    - `grep 'ApiError' frontend/src/types/api.ts` возвращает совпадение
    - `grep 'apiClient' frontend/src/api/client.ts` возвращает совпадение
    - `grep 'getInitData' frontend/src/api/client.ts` возвращает совпадение
    - `grep 'window.Telegram' frontend/src/api/client.ts` — getInitData читает только из window.Telegram.WebApp.initData
    - npm run typecheck в frontend/ завершается с кодом 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Создать компоненты состояний (Loading, Error, Unregistered, ErrorBoundary)</name>
  <files>
    frontend/src/components/LoadingScreen.tsx
    frontend/src/components/ErrorScreen.tsx
    frontend/src/components/UnregisteredScreen.tsx
    frontend/src/components/ErrorBoundary.tsx
  </files>
  <read_first>
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-UI-SPEC.md (разделы §8 Loading State, §9 Error State, §10 Unregistered User Screen, Copywriting Contract)
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-CONTEXT.md (D-14, D-16, D-17)
    - frontend/src/types/api.ts (создан в Task 1)
  </read_first>
  <action>
**frontend/src/components/LoadingScreen.tsx** — CSS-спиннер (без библиотек):
```typescript
/**
 * Экран загрузки — CSS-спиннер.
 * Показывается пока оба /api/me и /api/config в полёте.
 * aria-label на русском (FE-06).
 */
export function LoadingScreen() {
  return (
    <div
      className="min-h-dvh flex items-center justify-center bg-[var(--tg-theme-secondary-bg-color)]"
      role="status"
      aria-label="Загрузка..."
    >
      <div className="relative w-8 h-8">
        <div
          className="absolute inset-0 rounded-full border-2 border-[var(--tg-theme-hint-color)] opacity-25"
        />
        <div
          className="absolute inset-0 rounded-full border-2 border-transparent border-t-[var(--tg-theme-button-color)]"
          style={{ animation: 'spin 0.8s linear infinite' }}
        />
      </div>
      <style>{`
        @keyframes spin {
          to { transform: rotate(360deg); }
        }
      `}</style>
    </div>
  )
}
```

**frontend/src/components/ErrorScreen.tsx** — ошибка сети/сервера + «Повторить»:
```typescript
import { WifiOff } from 'lucide-react'

interface ErrorScreenProps {
  onRetry: () => void
  message?: string
}

/**
 * Экран ошибки — сетевые ошибки и 5xx.
 * onRetry — callback для повторного запроса (FE-08, D-17).
 */
export function ErrorScreen({ onRetry, message }: ErrorScreenProps) {
  return (
    <div className="min-h-dvh flex flex-col items-center justify-center gap-4 px-4 bg-[var(--tg-theme-secondary-bg-color)]">
      <WifiOff
        size={48}
        className="text-[var(--tg-theme-hint-color)]"
        aria-hidden="true"
      />
      <div className="text-center space-y-2">
        <h1 className="text-xl font-semibold text-[var(--tg-theme-text-color)]">
          Не удалось загрузить данные
        </h1>
        <p className="text-base text-[var(--tg-theme-hint-color)]">
          {message ?? 'Проверьте соединение и попробуйте снова'}
        </p>
      </div>
      <button
        onClick={onRetry}
        className="mt-2 h-12 px-8 rounded-xl bg-[var(--tg-theme-button-color)] text-[var(--tg-theme-button-text-color)] text-base font-semibold"
        aria-label="Повторить загрузку"
      >
        Повторить
      </button>
    </div>
  )
}
```

**frontend/src/components/UnregisteredScreen.tsx** — 404 заглушка (AUTH-04, D-14):
```typescript
import { UserX } from 'lucide-react'

/**
 * Экран для незарегистрированного юзера (API 404).
 * Показывает инструкцию /start в боте.
 * Никаких кнопок — юзер должен идти в бот (D-14).
 */
export function UnregisteredScreen() {
  return (
    <div className="min-h-dvh flex flex-col items-center justify-center gap-4 px-4 bg-[var(--tg-theme-secondary-bg-color)]">
      <UserX
        size={48}
        className="text-[var(--tg-theme-hint-color)]"
        aria-hidden="true"
      />
      <div className="text-center space-y-2">
        <h1 className="text-xl font-semibold text-[var(--tg-theme-text-color)]">
          Аккаунт не найден
        </h1>
        <p className="text-base text-[var(--tg-theme-hint-color)]">
          Пройдите /start в боте @shine_connect_bot
        </p>
      </div>
    </div>
  )
}
```

**frontend/src/components/ErrorBoundary.tsx** — React Error Boundary (FE-08):
```typescript
import { Component, ReactNode } from 'react'
import { WifiOff } from 'lucide-react'

interface Props {
  children: ReactNode
}

interface State {
  hasError: boolean
  error: Error | null
}

/**
 * React Error Boundary — перехватывает ошибки рендеринга.
 * Показывает fallback вместо пустого экрана (FE-08).
 */
export class ErrorBoundary extends Component<Props, State> {
  constructor(props: Props) {
    super(props)
    this.state = { hasError: false, error: null }
  }

  static getDerivedStateFromError(error: Error): State {
    return { hasError: true, error }
  }

  handleReload = () => {
    window.location.reload()
  }

  render() {
    if (this.state.hasError) {
      return (
        <div className="min-h-dvh flex flex-col items-center justify-center gap-4 px-4 bg-[var(--tg-theme-secondary-bg-color)]">
          <WifiOff size={48} className="text-[var(--tg-theme-hint-color)]" aria-hidden="true" />
          <div className="text-center space-y-2">
            <h1 className="text-xl font-semibold text-[var(--tg-theme-text-color)]">
              Произошла ошибка
            </h1>
            <p className="text-base text-[var(--tg-theme-hint-color)]">
              Перезагрузите приложение
            </p>
          </div>
          <button
            onClick={this.handleReload}
            className="mt-2 h-12 px-8 rounded-xl bg-[var(--tg-theme-button-color)] text-[var(--tg-theme-button-text-color)] text-base font-semibold"
            aria-label="Перезагрузить приложение"
          >
            Перезагрузить
          </button>
        </div>
      )
    }

    return this.props.children
  }
}
```

Обновить `frontend/src/main.tsx` — добавить ErrorBoundary:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import { SDKProvider } from '@telegram-apps/sdk-react'
import { ErrorBoundary } from './components/ErrorBoundary'
import './index.css'
import App from './App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <ErrorBoundary>
      <SDKProvider acceptCustomStyles>
        <App />
      </SDKProvider>
    </ErrorBoundary>
  </StrictMode>,
)
```
  </action>
  <verify>
    <automated>cd /path/to/frontend && npm run typecheck 2>&amp;1; echo "Exit: $?"</automated>
  </verify>
  <done>
    - Все 4 компонента созданы и TypeScript-чистые
    - LoadingScreen содержит CSS-анимацию spin без внешних библиотек
    - ErrorScreen принимает onRetry prop и показывает «Повторить»
    - UnregisteredScreen показывает «Аккаунт не найден» и «Пройдите /start в боте @shine_connect_bot»
    - ErrorBoundary обёртывает App в main.tsx
    - npm run typecheck проходит без ошибок
  </done>
  <acceptance_criteria>
    - `grep 'Загрузка...' frontend/src/components/LoadingScreen.tsx` возвращает совпадение
    - `grep '@keyframes spin' frontend/src/components/LoadingScreen.tsx` возвращает совпадение — нет внешних анимационных библиотек
    - `grep 'onRetry' frontend/src/components/ErrorScreen.tsx` возвращает совпадение
    - `grep 'Повторить' frontend/src/components/ErrorScreen.tsx` возвращает совпадение
    - `grep 'Аккаунт не найден' frontend/src/components/UnregisteredScreen.tsx` возвращает совпадение
    - `grep '@shine_connect_bot' frontend/src/components/UnregisteredScreen.tsx` возвращает совпадение
    - `grep 'getDerivedStateFromError' frontend/src/components/ErrorBoundary.tsx` возвращает совпадение
    - `grep 'ErrorBoundary' frontend/src/main.tsx` возвращает совпадение
    - `grep 'VPN\|прокси\|обход' frontend/src/components/*.tsx` НЕ возвращает совпадений — запрещённые слова отсутствуют
    - npm run typecheck в frontend/ завершается с кодом 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| window.Telegram.WebApp → client.ts | initData читается из Telegram runtime — единственный допустимый источник |
| API Response → TypeScript | JSON ответы бэкенда кастятся к TypeScript типам — несоответствие типов не вызовет XSS (React рендерит как text) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Spoofing | getInitData() в client.ts | mitigate | Функция читает ТОЛЬКО window.Telegram.WebApp.initData. Запрещено: передавать initData как параметр функции, хранить в localStorage, конструировать вручную |
| T-02-09 | Information Disclosure | ApiError messages | accept | Сообщения об ошибках — общие, не раскрывают внутреннюю структуру (нет stack trace в UI) |
| T-02-10 | Tampering | TypeScript cast в apiFetch | accept | React JSX экранирует HTML-сущности при рендеринге строк — XSS через JSON ответ невозможен при использовании JSX (без dangerouslySetInnerHTML) |
</threat_model>

<verification>
После выполнения всех задач плана:
1. `npm run typecheck` в frontend/ завершается с кодом 0
2. `grep 'X-Telegram-Init-Data' frontend/src/api/client.ts` находит совпадение
3. `grep 'Аккаунт не найден' frontend/src/components/UnregisteredScreen.tsx` находит совпадение
4. `grep 'ErrorBoundary' frontend/src/main.tsx` находит совпадение
5. `grep -r 'dangerouslySetInnerHTML' frontend/src/` НЕ находит совпадений
</verification>

<success_criteria>
- TypeScript типы точно отражают Pydantic схемы бэкенда (UserProfile, ConfigResponse)
- apiClient.getMe() и apiClient.getConfig() добавляют X-Telegram-Init-Data к каждому fetch
- initData берётся только из window.Telegram.WebApp.initData, никогда из других источников
- Loading, Error, Unregistered экраны реализованы с копирайтингом из UI-SPEC
- ErrorBoundary оборачивает всё приложение
- Нет запрещённых слов (VPN, прокси, обход) в UI компонентах
</success_criteria>

<output>
После завершения создать `.planning/phases/02-frontend-dashboard-react-mini-app/02-02-SUMMARY.md` с:
- Созданные файлы и их назначение
- TypeScript типы и их соответствие Pydantic моделям
- Скриншот или вывод typecheck
- Любые отклонения от плана
</output>
