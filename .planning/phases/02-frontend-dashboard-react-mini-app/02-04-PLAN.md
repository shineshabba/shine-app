---
phase: 02-frontend-dashboard-react-mini-app
plan: 04
type: execute
wave: 4
depends_on:
  - 02-03-PLAN.md
files_modified:
  - frontend/src/components/QrModal.tsx
  - frontend/src/components/SupportSheet.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - DASH-03
  - DASH-08
  - FE-03
  - FE-08

must_haves:
  truths:
    - "QR Modal показывает full-screen overlay с QR-кодом из qrcode.react"
    - "QR Modal закрывается тапом на backdrop или кнопкой ×"
    - "Bottom sheet выезжает снизу с CSS transition 300ms ease-out"
    - "Bottom sheet содержит 4 accordion-секции (iOS, Android, Windows, Mac)"
    - "Кнопка «Написать администратору» открывает t.me/shineshabba"
    - "App.tsx подключает QrModal и SupportSheet с реальными состояниями"
    - "npm run build завершается с кодом 0 и bundle gzipped < 300KB"
  artifacts:
    - path: "frontend/src/components/QrModal.tsx"
      provides: "Full-screen QR overlay"
      exports: ["QrModal"]
    - path: "frontend/src/components/SupportSheet.tsx"
      provides: "Bottom sheet с accordion и кнопкой админа"
      exports: ["SupportSheet"]
    - path: "frontend/src/App.tsx"
      provides: "Обновлённый App с подключёнными overlay компонентами"
  key_links:
    - from: "frontend/src/components/QrModal.tsx"
      to: "qrcode.react QRCodeSVG"
      via: "import"
      pattern: "QRCodeSVG"
    - from: "frontend/src/components/SupportSheet.tsx"
      to: "window.Telegram.WebApp.openTelegramLink"
      via: "onClick кнопки «Написать администратору»"
      pattern: "t.me/shineshabba"
    - from: "frontend/src/App.tsx"
      to: "QrModal и SupportSheet"
      via: "showQr/showSupport state"
      pattern: "showQr\|showSupport"
---

<objective>
Реализовать оверлеи: QR Modal (full-screen) и Bottom Sheet с accordion поддержки. Подключить в App.tsx.

Purpose: QR-код — ключевая функция для быстрого добавления конфига на устройство. Bottom sheet поддержки — инструкции по установке и связь с администратором. Без этих компонентов DASH-03 и DASH-08 не выполнены.

Output:
- `src/components/QrModal.tsx` — full-screen QR overlay с qrcode.react (SVG, ~15KB gzip)
- `src/components/SupportSheet.tsx` — bottom sheet с 4 accordion + кнопка «Написать администратору»
- `src/App.tsx` — обновлённый: QrModal и SupportSheet подключены с реальными состояниями
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-frontend-dashboard-react-mini-app/02-CONTEXT.md
@.planning/phases/02-frontend-dashboard-react-mini-app/02-UI-SPEC.md
@.planning/phases/02-frontend-dashboard-react-mini-app/02-03-SUMMARY.md

<interfaces>
<!-- Из frontend/src/App.tsx (Plan 03) -->
```typescript
const [showQr, setShowQr] = useState(false)
const [showSupport, setShowSupport] = useState(false)

// ConfigBlock.onShowQr → setShowQr(true)
// SupportButton.onOpen → setShowSupport(true)

// В рендере App — добавить:
// <QrModal vlessUrl={config.vless_url} isOpen={showQr} onClose={() => setShowQr(false)} />
// <SupportSheet isOpen={showSupport} onClose={() => setShowSupport(false)} />
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Создать QrModal — full-screen QR overlay</name>
  <files>
    frontend/src/components/QrModal.tsx
  </files>
  <read_first>
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-UI-SPEC.md (раздел §7 QR Modal — Full-Screen Overlay, Interaction States)
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-CONTEXT.md (D-02, D-09)
  </read_first>
  <action>
**frontend/src/components/QrModal.tsx** — full-screen overlay с QR (D-02, D-09):
```typescript
import { useEffect } from 'react'
import { QRCodeSVG } from 'qrcode.react'
import { X } from 'lucide-react'

interface QrModalProps {
  vlessUrl: string
  isOpen: boolean
  onClose: () => void
}

/**
 * Full-screen QR modal overlay (D-02, D-09, DASH-03).
 * - QR размером min(80vw, 80vh) — максимально большой
 * - Закрывается тапом на backdrop или кнопкой ×
 * - Fade-in 150ms / fade-out 150ms (UI-SPEC §7)
 * - role="dialog" + aria-label для accessibility
 * - focus trap: ESC закрывает (для Telegram Desktop)
 */
export function QrModal({ vlessUrl, isOpen, onClose }: QrModalProps) {
  // Закрытие по ESC (Telegram Desktop)
  useEffect(() => {
    if (!isOpen) return
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose()
    }
    document.addEventListener('keydown', handler)
    return () => document.removeEventListener('keydown', handler)
  }, [isOpen, onClose])

  // Блокировка скролла body пока modal открыт
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden'
    } else {
      document.body.style.overflow = ''
    }
    return () => { document.body.style.overflow = '' }
  }, [isOpen])

  if (!isOpen) return null

  return (
    <div
      role="dialog"
      aria-label="QR-код конфигурации"
      aria-modal="true"
      className="fixed inset-0 z-50 flex items-center justify-center"
      style={{
        backgroundColor: 'var(--tg-theme-bg-color)',
        animation: 'fadeIn 150ms ease-out',
      }}
      onClick={onClose}
    >
      <style>{`
        @keyframes fadeIn {
          from { opacity: 0; }
          to { opacity: 1; }
        }
      `}</style>

      {/* Кнопка закрытия × (44px touch target) */}
      <button
        onClick={(e) => { e.stopPropagation(); onClose() }}
        className="absolute top-4 right-4 flex items-center justify-center w-11 h-11 rounded-full text-[var(--tg-theme-hint-color)]"
        aria-label="Закрыть QR-код"
        style={{ background: 'none' }}
      >
        <X size={24} />
      </button>

      {/* QR код — не propagate click чтобы не закрывать при тапе на сам QR */}
      <div
        onClick={(e) => e.stopPropagation()}
        style={{ padding: '48px' }}
      >
        <QRCodeSVG
          value={vlessUrl}
          style={{
            width: 'min(80vw, 80vh)',
            height: 'min(80vw, 80vh)',
          }}
          level="M"
          bgColor="transparent"
          fgColor="var(--tg-theme-text-color)"
        />
      </div>
    </div>
  )
}
```
  </action>
  <verify>
    <automated>cd /path/to/frontend && npm run typecheck 2>&amp;1; echo "Exit: $?"</automated>
  </verify>
  <done>
    - QrModal рендерится с role="dialog" и aria-label
    - QRCodeSVG импортируется из qrcode.react
    - QR размер min(80vw, 80vh)
    - Клик на backdrop (div.fixed) вызывает onClose
    - Клик на сам QR НЕ вызывает onClose (stopPropagation)
    - Кнопка × вызывает onClose
    - ESC вызывает onClose
    - TypeScript чистый
  </done>
  <acceptance_criteria>
    - `grep 'QRCodeSVG' frontend/src/components/QrModal.tsx` возвращает совпадение
    - `grep 'qrcode.react' frontend/src/components/QrModal.tsx` возвращает совпадение
    - `grep 'role="dialog"' frontend/src/components/QrModal.tsx` возвращает совпадение
    - `grep 'min(80vw, 80vh)' frontend/src/components/QrModal.tsx` возвращает совпадение
    - `grep 'fixed inset-0' frontend/src/components/QrModal.tsx` возвращает совпадение
    - `grep 'Escape' frontend/src/components/QrModal.tsx` возвращает совпадение — ESC поддерживается
    - `grep 'stopPropagation' frontend/src/components/QrModal.tsx` возвращает совпадение — QR защищён от случайного закрытия
    - `grep 'overflow.*hidden' frontend/src/components/QrModal.tsx` возвращает совпадение — блокировка скролла
    - npm run typecheck завершается с кодом 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Создать SupportSheet и подключить оверлеи в App.tsx</name>
  <files>
    frontend/src/components/SupportSheet.tsx
    frontend/src/App.tsx
  </files>
  <read_first>
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-UI-SPEC.md (раздел §6 Bottom Sheet — Support, Interaction States, Copywriting Contract, Accordion sections)
    - .planning/phases/02-frontend-dashboard-react-mini-app/02-CONTEXT.md (D-03, D-18, D-19, D-20, specifics)
    - frontend/src/App.tsx (текущее состояние из Plan 03 — нужно читать перед изменением)
  </read_first>
  <action>
**frontend/src/components/SupportSheet.tsx** — bottom sheet с accordion (D-03, D-18, D-19, D-20):
```typescript
import { useState, useEffect } from 'react'
import { X, ChevronDown, ChevronRight, Smartphone, Tablet, Monitor, Laptop, MessageCircle } from 'lucide-react'

interface SupportSheetProps {
  isOpen: boolean
  onClose: () => void
}

const ADMIN_URL = 'https://t.me/shineshabba'

interface AccordionItem {
  id: string
  icon: React.ReactNode
  title: string
  content: string
}

const SUPPORT_ITEMS: AccordionItem[] = [
  {
    id: 'ios',
    icon: <Smartphone size={18} aria-hidden="true" />,
    title: 'iPhone / iPad',
    content: 'Скопируй конфигурацию → открой Streisand или Shadowrocket → нажми + → вставь ссылку → подключись',
  },
  {
    id: 'android',
    icon: <Tablet size={18} aria-hidden="true" />,
    title: 'Android',
    content: 'Скопируй конфигурацию → открой v2rayNG или Hiddify → нажми + → вставь ссылку → подключись',
  },
  {
    id: 'windows',
    icon: <Monitor size={18} aria-hidden="true" />,
    title: 'Windows',
    content: 'Скопируй конфигурацию → открой Hiddify или v2rayN → добавь сервер из буфера обмена → подключись',
  },
  {
    id: 'mac',
    icon: <Laptop size={18} aria-hidden="true" />,
    title: 'macOS',
    content: 'Скопируй конфигурацию → открой Hiddify или FoXray → добавь сервер из буфера обмена → подключись',
  },
]

/**
 * Bottom sheet с accordion инструкций и кнопкой админа (D-03, D-18, D-19, D-20, DASH-08).
 * - Выезжает снизу с CSS transform translateY 300ms ease-out
 * - Backdrop rgba(0,0,0,0.4) — тап закрывает
 * - max-height 80vh, внутренний контент скролится
 * - 4 accordion секции: iOS, Android, Windows, Mac (все закрыты по умолчанию)
 * - Кнопка «Написать администратору» → tg.openTelegramLink(t.me/shineshabba)
 */
export function SupportSheet({ isOpen, onClose }: SupportSheetProps) {
  const [openItem, setOpenItem] = useState<string | null>(null)

  // Сброс accordion при закрытии
  useEffect(() => {
    if (!isOpen) setOpenItem(null)
  }, [isOpen])

  // Блокировка скролла body
  useEffect(() => {
    if (isOpen) {
      document.body.style.overflow = 'hidden'
    } else {
      document.body.style.overflow = ''
    }
    return () => { document.body.style.overflow = '' }
  }, [isOpen])

  // ESC закрывает (Telegram Desktop)
  useEffect(() => {
    if (!isOpen) return
    const handler = (e: KeyboardEvent) => {
      if (e.key === 'Escape') onClose()
    }
    document.addEventListener('keydown', handler)
    return () => document.removeEventListener('keydown', handler)
  }, [isOpen, onClose])

  const handleAdminClick = () => {
    window.Telegram?.WebApp?.openTelegramLink(ADMIN_URL)
  }

  const toggleItem = (id: string) => {
    setOpenItem(prev => prev === id ? null : id)
  }

  return (
    <>
      {/* Backdrop */}
      <div
        className="fixed inset-0 z-40"
        style={{
          backgroundColor: 'rgba(0,0,0,0.4)',
          opacity: isOpen ? 1 : 0,
          pointerEvents: isOpen ? 'auto' : 'none',
          transition: 'opacity 300ms ease-out',
        }}
        onClick={onClose}
        aria-hidden="true"
      />

      {/* Sheet */}
      <div
        role="dialog"
        aria-label="Поддержка"
        aria-modal="true"
        className="fixed bottom-0 left-0 right-0 z-50 flex flex-col rounded-t-[20px] bg-[var(--tg-theme-bg-color)]"
        style={{
          maxHeight: '80vh',
          transform: isOpen ? 'translateY(0)' : 'translateY(100%)',
          transition: 'transform 300ms ease-out',
        }}
      >
        {/* Drag handle */}
        <div className="flex justify-center pt-3 pb-1">
          <div
            className="w-9 h-1 rounded-full"
            style={{ backgroundColor: 'color-mix(in srgb, var(--tg-theme-hint-color) 40%, transparent)' }}
            aria-hidden="true"
          />
        </div>

        {/* Header строка */}
        <div className="flex items-center justify-between px-4 py-3">
          <span className="text-xl font-semibold text-[var(--tg-theme-text-color)]">
            Поддержка
          </span>
          <button
            onClick={onClose}
            className="flex items-center justify-center w-11 h-11 text-[var(--tg-theme-hint-color)]"
            aria-label="Закрыть поддержку"
            style={{ background: 'none' }}
          >
            <X size={24} />
          </button>
        </div>

        {/* Скролируемый контент */}
        <div className="overflow-y-auto flex-1 px-4 pb-6">
          {/* Accordion */}
          <div className="space-y-1">
            {SUPPORT_ITEMS.map((item) => {
              const isExpanded = openItem === item.id
              return (
                <div key={item.id} className="rounded-xl overflow-hidden">
                  <button
                    onClick={() => toggleItem(item.id)}
                    className="w-full flex items-center gap-3 min-h-[44px] px-4 py-3 text-left text-base font-semibold text-[var(--tg-theme-text-color)]"
                    style={{ background: 'none' }}
                    aria-expanded={isExpanded}
                    aria-controls={`accordion-${item.id}`}
                  >
                    <span className="text-[var(--tg-theme-hint-color)]">{item.icon}</span>
                    <span className="flex-1">{item.title}</span>
                    {isExpanded
                      ? <ChevronDown size={18} className="text-[var(--tg-theme-hint-color)]" aria-hidden="true" />
                      : <ChevronRight size={18} className="text-[var(--tg-theme-hint-color)]" aria-hidden="true" />
                    }
                  </button>

                  <div
                    id={`accordion-${item.id}`}
                    style={{
                      overflow: 'hidden',
                      maxHeight: isExpanded ? '200px' : '0',
                      transition: 'max-height 200ms ease-out',
                    }}
                  >
                    <p className="px-4 pb-4 pt-1 text-sm text-[var(--tg-theme-hint-color)] leading-relaxed">
                      {item.content}
                    </p>
                  </div>
                </div>
              )
            })}
          </div>

          {/* Кнопка администратора (D-20) */}
          <button
            onClick={handleAdminClick}
            className="mt-6 w-full flex items-center justify-center gap-2 h-12 rounded-xl bg-[var(--tg-theme-button-color)] text-[var(--tg-theme-button-text-color)] text-base font-semibold"
            aria-label="Написать администратору в Telegram"
          >
            <MessageCircle size={18} aria-hidden="true" />
            Написать администратору
          </button>
        </div>
      </div>
    </>
  )
}
```

**frontend/src/App.tsx** — обновление: подключить QrModal и SupportSheet. Нужно ПРОЧИТАТЬ текущее состояние файла из Plan 03, затем добавить импорты и JSX для оверлеев в return-блок.

Добавить в начало файла (к существующим импортам):
```typescript
import { QrModal } from './components/QrModal'
import { SupportSheet } from './components/SupportSheet'
```

В конце return JSX (после закрывающего `</main>` и до `</div>`), раскомментировать/заменить placeholder комментарий на:
```typescript
{/* QR Modal (D-02, D-09) */}
{appState.status === 'ready' && appState.config && (
  <QrModal
    vlessUrl={appState.config.vless_url}
    isOpen={showQr}
    onClose={() => setShowQr(false)}
  />
)}

{/* Support Bottom Sheet (D-03) */}
<SupportSheet
  isOpen={showSupport}
  onClose={() => setShowSupport(false)}
/>
```

Также удалить неиспользуемый тип `AppOverlayHandlers` и `overlayHandlers` объект — заменить прямыми колбеками в props:
- `ConfigBlock onShowQr={() => setShowQr(true)}`
- `SupportButton onOpen={() => setShowSupport(true)}`
  </action>
  <verify>
    <automated>cd /path/to/frontend && npm run build 2>&amp;1; echo "Exit: $?"</automated>
  </verify>
  <done>
    - SupportSheet создан с 4 accordion-секциями и кнопкой «Написать администратору»
    - App.tsx содержит импорты QrModal и SupportSheet
    - showQr и showSupport состояния подключены к реальным компонентам
    - npm run build завершается с кодом 0
    - Размер bundle в dist/ ≤ 300KB gzip
  </done>
  <acceptance_criteria>
    - `grep 'QRCodeSVG\|qrcode.react' frontend/src/components/QrModal.tsx` возвращает совпадение
    - `grep 'shineshabba' frontend/src/components/SupportSheet.tsx` возвращает совпадение — URL администратора
    - `grep 'openTelegramLink' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'Написать администратору' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'iPhone / iPad' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'Android' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'Windows' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'macOS' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'translateY' frontend/src/components/SupportSheet.tsx` возвращает совпадение — CSS transition slide-up
    - `grep '300ms' frontend/src/components/SupportSheet.tsx` возвращает совпадение
    - `grep 'QrModal' frontend/src/App.tsx` возвращает совпадение
    - `grep 'SupportSheet' frontend/src/App.tsx` возвращает совпадение
    - `grep 'showQr' frontend/src/App.tsx` возвращает совпадение
    - `grep 'showSupport' frontend/src/App.tsx` возвращает совпадение
    - `npm run build` в frontend/ завершается с кодом 0
    - Вывод `npm run build` не содержит строк вида "XX kB" где XX > 300 для gzip итога (проверить в dist/assets/)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| VLESS URL → QRCodeSVG | vlessUrl из API передаётся в QR генератор — qrcode.react рендерит SVG без DOM инъекций |
| SupportSheet → openTelegramLink | URL администратора жёстко закодирован — не может быть изменён через API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-15 | Information Disclosure | QR Modal показывает полный VLESS URL | accept | Modal открывается только когда subscription_active=true (проверка в App.tsx). QRCodeSVG рендерит данные через SVG path — не innerHTML |
| T-02-16 | Tampering | Admin URL в SupportSheet | accept | `const ADMIN_URL = 'https://t.me/shineshabba'` — константа, не из API, не из пользовательского ввода |
| T-02-17 | Denial of Service | Много открытий bottom sheet | accept | CSS transition (не JS анимация) — не блокирует main thread. Оверлеи рендерятся только при isOpen=true (null return если закрыто — кроме sheet который использует CSS transform) |
</threat_model>

<verification>
После выполнения всех задач плана:
1. `npm run build` в frontend/ завершается с кодом 0
2. `npm run typecheck` в frontend/ завершается с кодом 0
3. `grep 'QrModal' frontend/src/App.tsx` находит совпадение
4. `grep 'SupportSheet' frontend/src/App.tsx` находит совпадение
5. `grep 'shineshabba' frontend/src/components/SupportSheet.tsx` находит совпадение
6. `ls frontend/dist/assets/` — gzip размер JS файлов суммарно ≤ 300KB
</verification>

<success_criteria>
- QR Modal: full-screen overlay, qrcode.react SVG, размер min(80vw,80vh), закрытие тапом/ESC/кнопкой
- SupportSheet: CSS slide-up 300ms, accordion 4 секций (все закрыты по умолчанию), «Написать администратору» → t.me/shineshabba
- App.tsx полностью подключён: showQr и showSupport управляют реальными компонентами
- npm run build завершается успешно, bundle ≤ 300KB gzip
- Все тексты на русском, нет запрещённых слов
</success_criteria>

<output>
После завершения создать `.planning/phases/02-frontend-dashboard-react-mini-app/02-04-SUMMARY.md` с:
- Список созданных компонентов
- Размер bundle (из npm run build output)
- Подтверждение что все требования DASH-03 и DASH-08 выполнены
- Любые отклонения от плана
</output>
